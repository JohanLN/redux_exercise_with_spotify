import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './component/App';

// Index.js est l'entryPoint de notre App. On entre donc directement dans APP.js

ReactDOM.render(
  <App />,
  document.getElementById('root')
);